spring:
  datasource:
    url: jdbc:mysql://app-2ca2c242a3116274688223b438a489aa-chart.50001-d12c22.svc.cluster.local:3306?serverTimezone=Asia/Seoul
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true # SQL 개행
        highlight_sql: true
        use_sql_comments: true # 실행되는 쿼리문 HINT 표시
    database-platform: org.hibernate.dialect.MySQL8Dialect
    defer-datasource-initialization: true
  sql:
    init:
      platform: mysql
      mode: always

logging:
  level:
    root: debug
